<h1>id:<%= @task.id %> の編集ページ！</h1>
<%= form_for(@task) do |t| %>
  <%= t.label :content,"修正入力" %>
  <%= t.text_field :content %>
  
  <%= t.submit "投稿"  %>

<% end %>

<%= link_to "一つ戻る", task_path %>
<%= link_to "一覧へ戻る", tasks_path %> 
  